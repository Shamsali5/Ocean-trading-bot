[pytest]
# Keep this file at the repository root so `pytest` run from the root
# picks up these settings and can import modules from `ocean_project/`.
pythonpath = ocean_project
testpaths = ocean_project/tests
